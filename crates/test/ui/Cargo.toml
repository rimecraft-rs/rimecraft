[package]
name = "rimecraft-client-ui-test"
version = "0.1.0"
edition = "2024"
authors = ["KrLite"]
description = "Essential tests for rimecraft-client-ui that require a context"
repository = "https://codeberg.org/DM-Earth/rimecraft/"
license = "AGPL-3.0-or-later"
categories = []

[badges]
maintenance = { status = "passively-maintained" }

[dependencies]
local-cx = { path = "../../core/local-cx", package = "rimecraft-local-cx" }
test-global = { path = "../global", package = "rimecraft-test-global", features = [
    "ui",
] }
keyboard = { path = "../../util/keyboard", package = "rimecraft-keyboard" }
mouse = { path = "../../util/mouse", package = "rimecraft-mouse" }
ui = { path = "../../client/ui", package = "rimecraft-client-ui" }

[lints]
workspace = true
